[project]
name = "home-api"
version = "0.1.0"
description = "Collection of APIs for home automation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.129.0",
    "pywa>=3.8.0",
    "requests>=2.32.5",
    "ruff>=0.15.1",
]
